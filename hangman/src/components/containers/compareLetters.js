import { Component } from 'react'
//import item from './AlphabetKeys'
import newWord from './newWord'


export default class compareLetters extends Component {
  render() { 
    return  console.log(compareLetters=(item)=> { newWord.includes(item )}
  
     ) }
}
